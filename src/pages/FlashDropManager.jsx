import AdminFlashDrops from "./AdminFlashDrops";

// Flash Drop Manager reuses the existing full-featured flash drop
// control center (create, schedule, templates, and QR verification).
export default function FlashDropManager() {
  return <AdminFlashDrops />;
}